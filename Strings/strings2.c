#include<stdio.h>

int main() {

    char st[] = "Srijan";

    printf("%s\n", st); 

// The %s format specifier, it will print the string until it encounters the null terminator '\0'.

        return 0;
}